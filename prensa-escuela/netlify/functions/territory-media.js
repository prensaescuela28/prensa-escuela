const {getStore}=require('@netlify/blobs');exports.handler=async e=>{const id=e.queryStringParameters?.id;if(!id)return{statusCode:400,body:'Falta id'};const s=getStore({name:'articles',siteID:process.env.SITE_ID,token:process.env.BLOBS_TOKEN,consistency:'strong'});const meta=await s.getMetadata(`territory-image:${id}`);if(!meta)return{statusCode:404,body:'No encontrada'};const b=await s.get(`territory-image:${id}`,{type:'arrayBuffer'});return{statusCode:200,headers:{'Content-Type':meta.metadata?.contentType||'image/jpeg','Cache-Control':'public,max-age=3600'},body:Buffer.from(b).toString('base64'),isBase64Encoded:true};};
