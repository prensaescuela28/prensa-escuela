const {getStore}=require('@netlify/blobs');const CORS={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type,x-press-password','Access-Control-Allow-Methods':'GET,POST,PUT,DELETE,OPTIONS'};const s=()=>getStore({name:'articles',siteID:process.env.SITE_ID,token:process.env.BLOBS_TOKEN,consistency:'strong'});const auth=e=>(e.headers['x-press-password']||e.headers['X-Press-Password'])===process.env.PRESS_PASSWORD;const json=(n,b)=>({statusCode:n,headers:{...CORS,'Content-Type':'application/json'},body:JSON.stringify(b)});
exports.handler=async e=>{const st=s();if(e.httpMethod==='OPTIONS')return{statusCode:200,headers:CORS,body:''};if(e.httpMethod==='GET'){const idx=(await st.get('territory-index',{type:'json'}))||[];return json(200,await Promise.all(idx.map(x=>st.get(`territory:${x.id}`,{type:'json'}))).then(a=>a.filter(Boolean)));}if(!auth(e))return json(401,{error:'No autorizado.'});let d={};try{d=JSON.parse(e.body||'{}')}catch{return json(400,{error:'Datos inválidos.'})};if(e.httpMethod==='POST'){if(!d.title||!d.place||!d.content)return json(400,{error:'Completa título, lugar y relato.'});const id=`territory-${Date.now()}`,item={id,title:String(d.title).trim(),place:String(d.place).trim(),author:String(d.author||'').trim(),category:String(d.category||'Historia local').trim(),content:String(d.content).trim(),lat:Number(d.lat)||null,lng:Number(d.lng)||null,createdAt:new Date().toISOString()};await st.setJSON(`territory:${id}`,item);const idx=(await st.get('territory-index',{type:'json'}))||[];idx.unshift({id,title:item.title,place:item.place,createdAt:item.createdAt});await st.setJSON('territory-index',idx);return json(200,item);}if(e.httpMethod==='DELETE'){const id=e.queryStringParameters?.id||d.id;await st.delete(`territory:${id}`);const idx=(await st.get('territory-index',{type:'json'}))||[];await st.setJSON('territory-index',idx.filter(x=>x.id!==id));return json(200,{ok:true});}return json(405,{error:'Método no permitido.'});};
